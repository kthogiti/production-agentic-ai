[project]
name = "ai-dev-team"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kthogiti", email = "kthogiti@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.3.18",
    "langchain-openai>=1.6.0",
    "langgraph>=1.2.11",
    "pydantic>=2.13.5",
    "python-dotenv>=1.2.3",
]

[project.scripts]
ai-dev-team = "ai_dev_team:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
